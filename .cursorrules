# Global AI Instructions

## Git & Version Control Policy
**CRITICAL:** NEVER run `git commit` or `git push` autonomously. 
You must **always** ask for the user's explicit approval before staging, committing, or pushing any code to the repository. No exceptions.
